<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register | MyBlog</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        :root {
            --primary-teal: #20A39E;
            --secondary-teal:rgb(14, 96, 109);
            --dark-teal:rgb(27, 153, 153);
            --navy-blue: #0A2463;
            --orange-accent:rgb(253, 161, 48);
            --light-bg: #F8F9FA;
            --dark-text: #333333;
        }

        body {
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 2rem 0;
        }

        .register-container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .register-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .register-left {
            background: linear-gradient(135deg, var(--navy-blue), var(--primary-teal));
            color: white;
            padding: 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            overflow: hidden;
            min-height: 600px;
        }

        .register-left::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="white" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
        }

        .register-left > * {
            position: relative;
            z-index: 1;
        }

        .register-right {
            padding: 3rem;
            max-height: 80vh;
            overflow-y: auto;
        }

        .brand-logo {
            font-size: 2.5rem;
            font-weight: 500;
            color: var(--primary-teal);
            margin-bottom: 0.5rem;
        }

        h1.welcome-text {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--teal-accent);
            margin-bottom: 0.5rem;
        }

        .subtitle-text {
            color: #6c757d;
            margin-bottom: 2rem;
        }

        .form-floating {
            margin-bottom: 1.5rem;
        }

        .form-floating > .form-control {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 1rem 0.75rem;
            height: auto;
            transition: all 0.3s ease;
        }

        .form-floating > .form-control:focus {
            box-shadow: 0 0 0 0.1rem rgba(96, 240, 235, 0.3);
        }

        .form-floating > label {
            color: #6c757d;
            font-weight: 500;
        }

        .form-floating > textarea.form-control {
            min-height: 100px;
            resize: vertical;
        }

         {
            background: linear-gradient(45deg, var(--primary-teal), var(--secondary-teal));
            border: none;
            color: white;
            padding: 0.875rem 2rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            width: 100%;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(32, 163, 158, 0.3);
        }

        .btn-register {
            background: transparent;
            border: 2px solid var(--dark-teal);
            color: var(--dark-teal);
            padding: 0.875rem 2rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            width: 100%;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-register:hover {
            background: var(--dark-teal);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(32, 163, 158, 0.3);
        }

        .btn-login{
            background: transparent;
            border: 2px solid var(--orange-accent);
            color: var(--orange-accent);
            padding: 0.875rem 2rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            width: 100%;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-login:hover {
            background: var(--orange-accent);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(32, 163, 158, 0.3);
        }

        .divider {
            text-align: center;
            margin: 1.5rem 0;
            position: relative;
        }

        .divider::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 1px;
            background: #dee2e6;
        }

        .divider span {
            background: white;
            padding: 0 1rem;
            color: #6c757d;
            font-size: 0.9rem;
        }

        .password-toggle {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: #6c757d;
            cursor: pointer;
            z-index: 10;
        }

        .password-toggle:hover {
            color: var(--primary-teal);
        }

        .feature-item {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
            opacity: 0.9;
        }

        .feature-item i {
            font-size: 1.2rem;
            margin-right: 0.75rem;
            color: rgba(255, 255, 255, 0.8);
        }

        .section-divider {
            border-top: 2px solid #e9ecef;
            margin: 2rem 0 1.5rem 0;
            padding-top: 1.5rem;
            position: relative;
        }

        .section-title {
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: white;
            padding: 0 1rem;
            color: var(--primary-teal);
            font-weight: 600;
            font-size: 0.9rem;
        }

        .profile-preview {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--primary-teal);
            margin: 0 auto 1rem;
            display: block;
        }

        .preview-container {
            text-align: center;
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .register-left {
                padding: 2rem;
                min-height: 200px;
            }
            
            .register-right {
                padding: 2rem;
                max-height: none;
            }
            
            .welcome-text {
                font-size: 1.5rem;
            }
        }

        .required-field::after {
            content: " *";
            color: #dc3545;
        }

        .optional-field {
            opacity: 0.8;
        }

        .form-text {
            color: #6c757d;
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="register-card">
            <div class="row g-0">
                <!-- Left Side - Branding -->
                <div class="col-lg-5 d-none d-lg-block">
                    <div class="register-left">
                        <div>
                            <div class="brand-logo mb-4">
                                <i class="bi bi-journal-richtext"></i>
                            </div>
                            <h2 class="h3 mb-3">Join MyBlog</h2>
                            <p class="mb-4 opacity-75">Create your account and start sharing your amazing stories with our community of writers and readers.</p>
                            
                            <div class="text-start">
                                <div class="feature-item">
                                    <i class="bi bi-person-plus"></i>
                                    <span>Create your unique profile</span>
                                </div>
                                <div class="feature-item">
                                    <i class="bi bi-pencil-square"></i>
                                    <span>Write and publish stories</span>
                                </div>
                                <div class="feature-item">
                                    <i class="bi bi-people"></i>
                                    <span>Connect with other writers</span>
                                </div>
                                <div class="feature-item">
                                    <i class="bi bi-heart"></i>
                                    <span>Engage with amazing content</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Right Side - Register Form -->
                <div class="col-lg-7">
                    <div class="register-right">
                        <!-- Mobile Brand -->
                        <div class="d-lg-none text-center mb-4">
                            <div class="brand-logo">MyBlog</div>
                        </div>

                        <h1 class="welcome-text">Create Account</h1>
                        <p class="subtitle-text">Fill in your details to get started with your blogging journey.</p>

                        <!-- Register Form -->
                        <form action="/auth/register" method="POST" id="registerForm">
                            
                            <!-- Profile Picture Section -->
                            <div class="preview-container">
                                <img src="/images/default-avatar.png" 
                                     alt="Profile Preview" 
                                     class="profile-preview" 
                                     id="profilePreview">
                            </div>

                            <div class="form-floating">
                                <input type="url" 
                                       class="form-control" 
                                       id="profilePictureUrl" 
                                       name="profilePictureUrl" 
                                       placeholder="https://example.com/your-image.jpg"
                                       onchange="updateProfilePreview(this.value)">
                                <label for="profilePictureUrl" class="optional-field">
                                    <i class="bi bi-image me-2"></i>Profile Picture URL
                                </label>
                                <div class="form-text">Optional: Add a URL to your profile picture</div>
                            </div>

                            <!-- Basic Information -->
                            <div class="form-floating">
                                <input type="text" 
                                       class="form-control" 
                                       id="name" 
                                       name="name" 
                                       placeholder="Your Name" 
                                       required>
                                <label for="name" class="required-field">
                                    <i class="bi bi-person me-2"></i>Full Name
                                </label>
                            </div>

                            <div class="form-floating">
                                <input type="email" 
                                       class="form-control" 
                                       id="email" 
                                       name="email" 
                                       placeholder="name@example.com" 
                                       required>
                                <label for="email" class="required-field">
                                    <i class="bi bi-envelope me-2"></i>Email Address
                                </label>
                            </div>

                            <div class="form-floating position-relative">
                                <input type="password" 
                                       class="form-control" 
                                       id="password" 
                                       name="password" 
                                       placeholder="Password" 
                                       required
                                       minlength="6">
                                <label for="password" class="required-field">
                                    <i class="bi bi-lock me-2"></i>Password
                                </label>
                                <button type="button" class="password-toggle" onclick="togglePassword()">
                                    <i class="bi bi-eye" id="toggleIcon"></i>
                                </button>
                                <div class="form-text">Minimum 6 characters required</div>
                            </div>

                            <!-- Professional Information -->
                            <div class="section-divider">
                                <span class="section-title">Professional Details</span>
                            </div>

                            <div class="form-floating">
                                <input type="text" 
                                       class="form-control" 
                                       id="title" 
                                       name="title" 
                                       placeholder="Your Title">
                                <label for="title" class="optional-field">
                                    <i class="bi bi-briefcase me-2"></i>Professional Title
                                </label>
                                <div class="form-text">e.g., Software Developer, Writer, Designer</div>
                            </div>

                            <div class="form-floating">
                                <textarea class="form-control" 
                                          id="bio" 
                                          name="bio" 
                                          placeholder="Tell us about yourself"
                                          style="height: 100px"></textarea>
                                <label for="bio" class="optional-field">
                                    <i class="bi bi-chat-quote me-2"></i>Bio
                                </label>
                                <div class="form-text">Share a brief description about yourself</div>
                            </div>

                            <!-- Social Links -->
                            <div class="section-divider">
                                <span class="section-title">Social Links</span>
                            </div>

                            <div class="form-floating">
                                <input type="url" 
                                       class="form-control" 
                                       id="githubUrl" 
                                       name="githubUrl" 
                                       placeholder="https://github.com/username">
                                <label for="githubUrl" class="optional-field">
                                    <i class="bi bi-github me-2"></i>GitHub URL
                                </label>
                            </div>

                            <div class="form-floating">
                                <input type="url" 
                                       class="form-control" 
                                       id="twitterUrl" 
                                       name="twitterUrl" 
                                       placeholder="https://twitter.com/username">
                                <label for="twitterUrl" class="optional-field">
                                    <i class="bi bi-twitter me-2"></i>Twitter URL
                                </label>
                            </div>

                            <div class="form-floating">
                                <input type="url" 
                                       class="form-control" 
                                       id="linkedinUrl" 
                                       name="linkedinUrl" 
                                       placeholder="https://linkedin.com/in/username">
                                <label for="linkedinUrl" class="optional-field">
                                    <i class="bi bi-linkedin me-2"></i>LinkedIn URL
                                </label>
                            </div>

                            <!-- Terms and Conditions -->
                            <div class="form-check mb-4">
                                <input class="form-check-input" type="checkbox" id="agreeTerms" required>
                                <label class="form-check-label text-muted" for="agreeTerms">
                                    I agree to the <a href="/terms" class="text-decoration-none" style="color: var(--primary-teal);">Terms of Service</a> 
                                    and <a href="/privacy" class="text-decoration-none" style="color: var(--primary-teal);">Privacy Policy</a>
                                </label>
                            </div>

                            <button type="submit" class="btn btn-register">
                                <i class="bi bi-person-plus me-2"></i>
                                Create Account
                            </button>

                            <div class="divider">
                                <span>or</span>
                            </div>

                            <a href="/auth/login" class="btn btn-login">
                                <i class="bi bi-box-arrow-in-right me-2"></i>
                                Already have an account? Sign In
                            </a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Password toggle functionality
        function togglePassword() {
            const passwordInput = document.getElementById('password');
            const toggleIcon = document.getElementById('toggleIcon');
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                toggleIcon.className = 'bi bi-eye-slash';
            } else {
                passwordInput.type = 'password';
                toggleIcon.className = 'bi bi-eye';
            }
        }

        // Profile picture preview
        function updateProfilePreview(url) {
            const preview = document.getElementById('profilePreview');
            if (url && isValidUrl(url)) {
                preview.src = url;
                preview.onerror = function() {
                    this.src = '/images/default-avatar.png';
                };
            } else {
                preview.src = '/images/default-avatar.png';
            }
        }

        function isValidUrl(string) {
            try {
                new URL(string);
                return true;
            } catch (_) {
                return false;
            }
        }

        // Form validation and loading state
        document.getElementById('registerForm').addEventListener('submit', function(e) {
            const submitBtn = this.querySelector('button[type="submit"]');
            const originalText = submitBtn.innerHTML;
            
            submitBtn.innerHTML = '<i class="bi bi-arrow-clockwise spin me-2"></i>Creating Account...';
            submitBtn.disabled = true;
            
            // Re-enable button after 5 seconds in case of error
            setTimeout(() => {
                submitBtn.innerHTML = originalText;
                submitBtn.disabled = false;
            }, 5000);
        });

        // Add floating animation to form elements
        document.addEventListener('DOMContentLoaded', function() {
            const formControls = document.querySelectorAll('.form-control');
            
            formControls.forEach(control => {
                control.addEventListener('focus', function() {
                    this.parentElement.style.transform = 'translateY(-2px)';
                });
                
                control.addEventListener('blur', function() {
                    this.parentElement.style.transform = 'translateY(0)';
                });
            });

            // Auto-resize textarea
            const textarea = document.getElementById('bio');
            textarea.addEventListener('input', function() {
                this.style.height = 'auto';
                this.style.height = Math.max(100, this.scrollHeight) + 'px';
            });
        });

        // Real-time form validation
        document.getElementById('email').addEventListener('blur', function() {
            const email = this.value;
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            
            if (email && !emailRegex.test(email)) {
                this.setCustomValidity('Please enter a valid email address');
                this.classList.add('is-invalid');
            } else {
                this.setCustomValidity('');
                this.classList.remove('is-invalid');
            }
        });

        document.getElementById('password').addEventListener('input', function() {
            const password = this.value;
            
            if (password.length > 0 && password.length < 6) {
                this.setCustomValidity('Password must be at least 6 characters long');
                this.classList.add('is-invalid');
            } else {
                this.setCustomValidity('');
                this.classList.remove('is-invalid');
            }
        });
    </script>

    <style>
        .spin {
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        
        .form-floating {
            transition: transform 0.2s ease;
        }

        .is-invalid {
            border-color: #dc3545 !important;
        }
    </style>
</body>
</html>
