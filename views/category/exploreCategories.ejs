<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>topics</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bKxP1h9dJPCxRkF" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header') %>

<section class="explore-topics-section">
    <div class="topics-header">
        <h1>Explore Topics</h1>
        <p>Find articles based on subjects that interest you. Click a topic to see all related posts.</p>
    </div>

    <div class="topics-scroll-container">
        <div class="topics-list">
            <% if (categories && categories.length > 0) { %>
                <% categories.forEach(category => { %>
                    <a href="/category/<%= category.id %>" class="topic-pill">
                        <%= category.name %>
                    </a>
                <% }) %>
            <% } else { %>
                <p class="no-topics-message">No topics available yet.</p>
            <% } %>
        </div>
        </div>
</section>

<%- include('../partials/footer', { categories: categories }) %>
</body>
</html>