<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top shadow-sm">
    <div class="container-fluid">
        <div class="d-flex align-items-center me-auto">
            <a href="/" class="logo">📝 MyBlog</a>
            <form class="d-flex ms-3" action="/posts/search" method="GET">
                <input class="form-control me-2" type="search" placeholder="Search posts..." aria-label="Search" name="search">
                <button class="btn-search-header" type="submit">Search</button>
            </form>
        </div>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <% if (currentUser) { %>
                <li class="nav-item me-3">
                    <a class="nav-link" href="/posts/new" title="Write a new post">
                        <i class="bi bi-pencil-square fs-5"></i> </a>
                </li>
                <% } %>

                <li class="nav-item">
                    <a class="nav-link" href="/posts">All Posts</a>
                </li>
                <% if (currentUser) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/posts/myPosts">My Posts</a>
                </li>
                <% } %>
                <li class="nav-item">
                    <a class="nav-link" href="/topics">Categories</a>
                </li>
                <% if (!currentUser) { %>
                <li class="nav-item">
                    <a class="login-button" href="/auth/login">Login / Signup</a>
                </li>
                <% } else { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle fs-5 me-1"></i> <%= currentUser.name %>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="/auth/profile/<%= currentUser.id %>">Profile</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="/auth/logout">Logout</a></li>
                    </ul>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
</header>
</body>
</html>